name: Network
main: com.octopod.network.NetworkPlugin
version: ${buildNumber}
softdepend: ['LilyPad-Connect']
permissions:
    network.server.connect:
        default: true
    network.server.list:
        default: true
    network.main:
        default: true