name: NetworkPlus
main: com.octopod.network.NetworkPlugin
version: ${version}
softdepend: ['LilyPad-Connect']
permissions:
    network.server.connect:
        default: true
    network.server.list:
        default: true
    network.main:
        default: true